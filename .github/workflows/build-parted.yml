name: Build Static sgdisk for Android arm64 (fully patched no uuid no ncurses)

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y wget tar xz-utils build-essential g++ make pkg-config

    - name: Download Android NDK r25c
      run: |
        wget -q https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
        unzip -q android-ndk-r25c-linux.zip -d $HOME
        echo "ANDROID_NDK_HOME=$HOME/android-ndk-r25c" >> $GITHUB_ENV

    - name: Download gptfdisk 1.0.10 source
      run: |
        wget https://downloads.sourceforge.net/project/gptfdisk/gptfdisk/1.0.10/gptfdisk-1.0.10.tar.gz
        tar xf gptfdisk-1.0.10.tar.gz
        cd gptfdisk-1.0.10

    - name: Patch guid.cc and guid.h to completely remove uuid dependency
      working-directory: gptfdisk-1.0.10
      run: |
        # 注释 include
        sed -i 's/#include <uuid\/uuid.h>/\/\/ #include <uuid\/uuid.h>/g' guid.cc guid.h

        # 删除 uuid_t 定义和相关函数调用
        sed -i '/typedef uuid_t my_uuid_t/d' guid.h
        sed -i '/my_uuid_t uuidData/d' guid.cc
        sed -i '/uuid_generate/d' guid.cc
        sed -i '/uuid_unparse/d' guid.cc
        sed -i '/uuid_clear/d' guid.cc
        sed -i '/uuid_copy/d' guid.cc
        sed -i '/uuid_is_null/d' guid.cc
        sed -i '/uuid_generate_time/d' guid.cc
        sed -i '/uuid_generate_random/d' guid.cc

        # 替换 UUID 生成逻辑为简单随机填充（避免 crash）
        sed -i '/GenerateGUID() {/a \    srand(time(NULL));\n    for (int i = 0; i < 16; i++) uuidData[i] = rand() % 256;' guid.cc
        sed -i '/GenerateGUID() {/a \    \/\/ Fallback random UUID without libuuid' guid.cc

        echo "Patched guid.cc and guid.h to remove all uuid dependencies"

    - name: Cross-compile static sgdisk for Android arm64
      working-directory: gptfdisk-1.0.10
      run: |
        export TOOLCHAIN=$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64
        export TARGET=aarch64-linux-android
        export API=21

        make \
          CC=$TOOLCHAIN/bin/$TARGET$API-clang \
          CXX=$TOOLCHAIN/bin/$TARGET$API-clang++ \
          AR=$TOOLCHAIN/bin/llvm-ar \
          LD=$TOOLCHAIN/bin/ld \
          RANLIB=$TOOLCHAIN/bin/llvm-ranlib \
          STRIP=$TOOLCHAIN/bin/llvm-strip \
          CXXFLAGS="-static -static-libgcc -static-libstdc++ -DNO_NCURSES -Wno-error" \
          LDFLAGS="-static" \
          sgdisk

        $TOOLCHAIN/bin/llvm-strip sgdisk

    - name: Verify binary
      run: |
        file gptfdisk-1.0.10/sgdisk
        ls -lh gptfdisk-1.0.10/sgdisk
        echo "Architecture check (should be aarch64):"
        file gptfdisk-1.0.10/sgdisk | grep aarch64 || echo "Not aarch64, check compile"

    - name: Upload static sgdisk
      uses: actions/upload-artifact@v4
      with:
        name: sgdisk-static-android-arm64-fully-patched
        path: gptfdisk-1.0.10/sgdisk
        retention-days: 7
